name: grid_search_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_lints: ^2.0.0
  flutter_native_splash:
  get: ^4.6.5
  sizer: ^2.0.15
  lottie: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter



flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/

flutter_native_splash:
  color: "#ffffff"
  image: "assets/images/gridlogo.png"
